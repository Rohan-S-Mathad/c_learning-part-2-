# include <stdio.h>

int main(){
     int marks[90];//it indicates that 90 intergers can be stored in marks variable
     marks[0]=45;
     marks[1]=95;
     printf (" Marks 0 and Marks 1 is %d  %d  ", marks[0],marks[1]  );
     return 0;
}
